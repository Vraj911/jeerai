================================================================================
JeerAI Backend — Scenario-Based Technical Interview Questions
Generated from: backend/jeerai-backend/ (Spring Boot 3, Java 17, PostgreSQL)
================================================================================

Each question references actual JeerAI code. Format:
  TOPIC | SCENARIO | QUESTION | WHAT IT TESTS | EXPECTED ANSWER | FOLLOW-UP | RED FLAG

================================================================================
CORE JAVA (7 questions)
================================================================================

--- Q1 ---
TOPIC: Core Java
SCENARIO: AiPromptBundle is declared as:
  public record AiPromptBundle(Prompt prompt, BeanOutputConverter<?> converter) {}
  Similar records exist: GenerateContext, IssueDraftOutput, AutomationEvent.IssueSnapshot.
QUESTION: Why use Java records here instead of a conventional class with getters and setters? What guarantees do records provide that matter for AI context objects passed between AiPromptBuilder and AiService?
WHAT IT TESTS: Records vs classes; immutability; value semantics; compact constructors.
EXPECTED ANSWER: Records are immutable data carriers with auto-generated equals/hashCode/toString and accessors. For AI context/output DTOs that are created, passed to Jackson serialization, and never mutated, records eliminate boilerplate and prevent accidental state changes. They clearly signal "this is pure data, not behavior."
FOLLOW-UP: Could you add a setter to a record component? What happens if IssueSnapshot is mutated after being placed on AutomationEvent?
RED FLAG: Says records are just syntactic sugar with no difference from Lombok @Data classes; cannot explain immutability or that records are implicitly final.

--- Q2 ---
TOPIC: Core Java
SCENARIO: AiContextService.loadGenerateContext() (lines 48-53):
  List<IssueSummary> issues = issueRepository.findByProjectId(projectId).stream()
      .filter(i -> isOpenStatus(i.getStatus()))
      .sorted(priorityThenUpdated())
      .limit(maxIssues)
      .map(this::toIssueSummary)
      .toList();
QUESTION: Identify which operations are intermediate vs terminal. When does the database get queried vs when does sorting happen? What changes if you replaced .toList() with .collect(Collectors.toList())?
WHAT IT TESTS: Stream pipeline; lazy evaluation; intermediate vs terminal operations; Java 16+ toList().
EXPECTED ANSWER: filter, sorted, limit, and map are intermediate (lazy); toList() is terminal and triggers the whole pipeline. The repository call happens first (eager list), then in-memory stream processing. toList() returns an unmodifiable list; Collectors.toList() returns a mutable ArrayList — functionally similar here but mutability differs.
FOLLOW-UP: What if findByProjectId returned a lazy Stream from the DB — would limit(maxIssues) push down to SQL?
RED FLAG: Claims sorted() runs at database level without a query change; cannot name any intermediate operation.

--- Q3 ---
TOPIC: Core Java
SCENARIO: JpaRepositoryMapper.toEntity(User) line 86-88:
  UserEntity entity = model.getId() == null
      ? new UserEntity()
      : userJpaRepository.findByPublicId(model.getId()).orElseGet(UserEntity::new);
  IssueService.getById() uses .orElseThrow(() -> new ResourceNotFoundException(...)).
QUESTION: Explain the difference between orElse, orElseGet, and orElseThrow. Why is orElseGet(UserEntity::new) correct here but orElse(new UserEntity()) would be problematic?
WHAT IT TESTS: Optional API; lazy vs eager supplier evaluation.
EXPECTED ANSWER: orElse always evaluates its argument even when Optional is present. orElseGet takes a Supplier and only runs it when empty. orElseThrow throws when empty. Using orElse(new UserEntity()) would create a throwaway UserEntity on every save of an existing user; orElseGet defers allocation until needed.
FOLLOW-UP: In InvitationService lines 66-70, why is .ifPresent() used instead of orElseThrow on the Optional?
RED FLAG: Treats all three as interchangeable; says orElseGet runs every time.

--- Q4 ---
TOPIC: Core Java
SCENARIO: AiContextService.priorityThenUpdated() lines 112-114:
  return Comparator.comparingInt(this::priorityIndex)
      .thenComparing(Issue::getUpdatedAt, Comparator.nullsLast(Comparator.reverseOrder()));
QUESTION: Walk through how two issues with priorities "high" and "medium" but null updatedAt timestamps would be ordered. Why nullsLast combined with reverseOrder?
WHAT IT TESTS: Comparator composition; method references; null-safe ordering.
EXPECTED ANSWER: comparingInt orders by priority index first (highest=0 before medium=2). thenComparing breaks ties by updatedAt descending (newest first). nullsLast ensures null updatedAt values sort after all non-null dates within the same priority band, avoiding NullPointerException.
FOLLOW-UP: How would you reverse the entire sort so lowest priority appears first but still newest-first within priority?
RED FLAG: Cannot explain chaining order; says null issues would crash without knowing nullsLast handles it.

--- Q5 ---
TOPIC: Core Java
SCENARIO: AutomationExecutionService.triggerMatches() lines 102-134 uses switch expressions with yield:
  case "status_change" -> {
      if (!"status_change".equals(event.getEventType())) { yield false; }
      yield v == null || v.isBlank() || (event.getAfter() != null && v.equalsIgnoreCase(event.getAfter().status()));
  }
QUESTION: How is this different from a classic switch statement with break? Could you rewrite the assignee_change arm using the arrow form without yield?
WHAT IT TESTS: Switch expressions (Java 14+); yield vs return; exhaustiveness.
EXPECTED ANSWER: Switch expressions must produce a value assigned to the return. yield exits a block arm and supplies the value; arrow arms without blocks use implicit return. assignee_change could use a single arrow expression if logic fits one expression, but multi-step checks need block + yield. Classic switch uses break and does not return a value from the switch itself.
FOLLOW-UP: What does the default arm do when an unknown trigger type appears in the database?
RED FLAG: Confuses yield with return from the method; thinks fall-through still happens in switch expressions.

--- Q6 ---
TOPIC: Core Java
SCENARIO: InvitationService lines 41, 207-210:
  private final SecureRandom secureRandom = new SecureRandom();
  secureRandom.nextBytes(bytes);
  return Base64.getUrlEncoder().withoutPadding().encodeToString(bytes);
QUESTION: Why SecureRandom instead of java.util.Random for invitation tokens? Why Base64 URL encoder without padding?
WHAT IT TESTS: Cryptographic vs statistical randomness; token safety in URLs.
EXPECTED ANSWER: Invitation tokens are security secrets; SecureRandom is cryptographically strong and unpredictable, while Random is deterministic and guessable. URL-safe Base64 avoids +/= characters that break URL paths; withoutPadding keeps tokens shorter for invite links.
FOLLOW-UP: How many bits of entropy does 24 bytes provide? Is that sufficient for invite tokens?
RED FLAG: Says Random is fine for tokens; suggests MD5 hash of email as token.

--- Q7 ---
TOPIC: Core Java
SCENARIO: IssueService.create() uses Instant.now() for createdAt/updatedAt (lines 115-116). JwtUtil uses java.util.Date for JWT claims. Domain models and ErrorResponse use Instant throughout.
QUESTION: Why does JeerAI prefer Instant in the domain layer? What problems does java.util.Date have?
WHAT IT TESTS: java.time API; UTC semantics; immutability of Instant.
EXPECTED ANSWER: Instant represents a point on the UTC timeline, is immutable, and avoids timezone confusion. Date is mutable, legacy, and mixes instant with calendar semantics. JeerAI stores timestamps in PostgreSQL as timestamp without timezone ambiguity when using Instant consistently in services.
FOLLOW-UP: How would you convert Instant to JWT's Date in one line for JwtUtil consistency?
RED FLAG: Says Date and Instant are identical; recommends Calendar.getInstance() for new code.

--- Q8 ---
TOPIC: Core Java
SCENARIO: IssueService.getAll() line 77: var accessibleWorkspaceIds = workspaceAccessService.getAccessibleWorkspaceIds();
QUESTION: What type does the compiler infer for accessibleWorkspaceIds? When is var inappropriate in JeerAI-style service code?
WHAT IT TESTS: Local variable type inference; readability tradeoffs.
EXPECTED ANSWER: Compiler infers Set<String> (from WorkspaceAccessService return type). var is fine when the RHS makes the type obvious. Inappropriate when the initializer is null, a primitive widening chain, or a complex generic where the explicit type documents intent (e.g., Map<String, List<Issue>>).
FOLLOW-UP: Would you use var for the return type of a public API method?
RED FLAG: Says var is dynamic typing like JavaScript; thinks var means Object.

================================================================================
SPRING BOOT (8 questions)
================================================================================

--- Q1 ---
TOPIC: Spring Boot
SCENARIO: AutomationExecutionService.onAutomationEvent():
  @TransactionalEventListener(phase = TransactionPhase.AFTER_COMMIT)
  @Transactional(propagation = Propagation.REQUIRES_NEW)
  IssueService.create() publishes AutomationEvent inside @Transactional method.
QUESTION: Why AFTER_COMMIT instead of a regular @EventListener? What happens to automation if the issue transaction rolls back?
WHAT IT TESTS: Transaction-bound events; listener phases; consistency.
EXPECTED ANSWER: AFTER_COMMIT ensures the listener runs only after the publishing transaction successfully commits, so automation sees persisted issue data. If the transaction rolls back, the listener never runs — no automation on uncommitted or failed creates. A default @EventListener might fire before commit and read uncommitted data.
FOLLOW-UP: What is TransactionPhase.BEFORE_COMMIT used for? Could you use AFTER_ROLLBACK here?
RED FLAG: Says listener runs inside the same transaction always; automation runs even on rollback.

--- Q2 ---
TOPIC: Spring Boot
SCENARIO: Same method — @Transactional(propagation = Propagation.REQUIRES_NEW) on onAutomationEvent(), with inline comment explaining the publishing transaction is already gone.
QUESTION: What transaction context exists when AFTER_COMMIT listener fires? What breaks if you remove REQUIRES_NEW?
WHAT IT TESTS: Propagation REQUIRES_NEW; transaction suspension; atomicity of automation side effects.
EXPECTED ANSWER: After commit, there is no active transaction on the thread. Without REQUIRES_NEW, each repository save in AutomationActionExecutor would auto-commit in its own implicit transaction — partial automation success with no rollback across actions. REQUIRES_NEW opens one new transaction wrapping the entire automation chain for atomicity.
FOLLOW-UP: Does REQUIRES_NEW join or suspend the outer transaction if one existed?
RED FLAG: Says REQUIRES_NEW is needed to run in the same transaction as IssueService.create().

--- Q3 ---
TOPIC: Spring Boot
SCENARIO: DatabaseConfig, all Jpa*RepositoryAdapter classes, and JpaRepositoryMapper carry @Profile("postgres"). application.properties sets spring.profiles.active=postgres.
QUESTION: What happens at startup if someone runs with profile "h2" or no profile? How does @Profile affect bean creation for IssueRepository?
WHAT IT TESTS: Spring profiles; conditional bean loading; test vs prod configuration.
EXPECTED ANSWER: @Profile("postgres") beans are only registered when the postgres profile is active. Without it, JpaIssueRepositoryAdapter and DatabaseConfig are absent — IssueRepository would fail injection unless another profile provides an implementation. Tests may use a different profile with alternate config.
FOLLOW-UP: How would you add an in-memory H2 profile for integration tests without duplicating adapters?
RED FLAG: Says @Profile is only for logging; thinks beans load regardless of profile.

--- Q4 ---
TOPIC: Spring Boot
SCENARIO: NoOpInvitationDeliveryService:
  @ConditionalOnProperty(prefix = "app.mail", name = "enabled", havingValue = "false", matchIfMissing = true)
  SmtpInvitationDeliveryService: havingValue = "true"
QUESTION: How many InvitationDeliveryService beans exist at runtime? What happens if both properties are somehow true?
WHAT IT TESTS: @ConditionalOnProperty; bean ambiguity; matchIfMissing.
EXPECTED ANSWER: Exactly one implementation should be active — mutually exclusive conditions. matchIfMissing=true makes NoOp the default when property is absent (local dev). If both matched, Spring would throw NoUniqueBeanDefinitionException on InvitationService injection.
FOLLOW-UP: Why is this better than @Profile("smtp") for mail toggling?
RED FLAG: Says both beans inject as a List without explaining conflict; confuses with @Value.

--- Q5 ---
TOPIC: Spring Boot
SCENARIO: IssueService has an 11-parameter constructor (lines 50-70) with no @Autowired on fields. SecurityConfig and JwtAuthenticationFilter use constructor injection similarly.
QUESTION: Why constructor injection over @Autowired field injection? How does Spring satisfy IssueService's ApplicationEventPublisher dependency?
WHAT IT TESTS: Dependency injection best practices; immutability; testability.
EXPECTED ANSWER: Constructor injection makes dependencies explicit, enables final fields, and allows unit tests to pass mocks without Spring context. Spring automatically provides ApplicationEventPublisher as a built-in bean when creating IssueService — no @Autowired needed on constructor for a single constructor class.
FOLLOW-UP: What does @RequiredArgsConstructor from Lombok generate for AutomationExecutionService?
RED FLAG: Recommends field injection for optional dependencies; says ApplicationEventPublisher must be @Bean-defined manually.

--- Q6 ---
TOPIC: Spring Boot
SCENARIO: AutomationExecutionService line 24: @Value("${app.automation.max-depth:3}") private int maxDepth;
QUESTION: What does the :3 syntax mean? What value is used if the property key is missing entirely vs set to empty string?
WHAT IT TESTS: Property placeholder resolution; default values; type conversion.
EXPECTED ANSWER: :3 is the default when app.automation.max-depth is undefined. Spring converts "3" to int. If property is missing, maxDepth=3. Empty string may cause conversion failure at startup. Explicit 0 in config would override default.
FOLLOW-UP: Where else in JeerAI are @Value defaults used? (AiContextService max-issues:50)
RED FLAG: Says application stops if property missing without default; confuses with Spring EL ${...}.

--- Q7 ---
TOPIC: Spring Boot
SCENARIO: SecurityConfig.securityFilterChain() lines 41-42:
  .addFilterBefore(privateNetworkAccessFilter(), CorsFilter.class)
  .addFilterBefore(jwtAuthenticationFilter, UsernamePasswordAuthenticationFilter.class)
  SessionCreationPolicy.STATELESS on line 33.
QUESTION: Why must PrivateNetworkAccessFilter run before CorsFilter? What does STATELESS mean for a JWT API?
WHAT IT TESTS: Filter chain order; CORS preflight; session management.
EXPECTED ANSWER: PNA header must be added to preflight responses before CORS filter processes OPTIONS; order determines which filter sees the request first. STATELESS means Spring Security never creates HttpSession — auth is per-request via JWT in Authorization header, saving server memory and fitting SPA architecture.
FOLLOW-UP: Where in the chain does JwtAuthenticationFilter run relative to authorization checks?
RED FLAG: Says filter order does not matter; thinks STATELESS disables authentication.

--- Q8 ---
TOPIC: Spring Boot
SCENARIO: GlobalExceptionHandler — separate handlers for ResourceNotFoundException (404), BadRequestException (400), AccessDeniedException (403), catch-all Exception (500). MethodArgumentNotValidException returns a Map with field errors.
QUESTION: If IssueService throws ResourceNotFoundException, which handler runs? If a NullPointerException escapes a controller, what does the client see?
WHAT IT TESTS: @RestControllerAdvice; @ExceptionHandler resolution; exception hierarchy.
EXPECTED ANSWER: ResourceNotFoundException matches handleNotFound — 404 with ErrorResponse JSON. NullPointerException is not a subclass of handled types, so handleUnexpected catches it — 500 with generic message "An unexpected error occurred" (no stack trace leaked). Most specific matching handler wins.
FOLLOW-UP: Why does handleValidation return a different body shape than other handlers?
RED FLAG: Says all exceptions return 500; thinks @ExceptionHandler is inherited like Java method overriding across controllers.

--- Q9 ---
TOPIC: Spring Boot
SCENARIO: DatabaseConfig:
  @EnableTransactionManagement
  @EntityScan(basePackages = "com.jeerai.backend.entity")
  @EnableJpaRepositories(basePackages = "com.jeerai.backend.repository.jpa")
QUESTION: What does each annotation enable? What fails if you remove @EntityScan but keep @EnableJpaRepositories?
WHAT IT TESTS: Spring Data JPA bootstrap; entity scanning vs repository scanning.
EXPECTED ANSWER: EnableTransactionManagement activates @Transactional AOP. EntityScan registers JPA entities for Hibernate. EnableJpaRepositories creates Spring Data proxy beans for *JpaRepository interfaces. Without EntityScan, entities might not be managed (depending on @SpringBootApplication scan path) — Hibernate may not map IssueEntity, causing startup or runtime failures.
FOLLOW-UP: Why are domain models in com.jeerai.backend.model NOT entity-scanned?
RED FLAG: Thinks @EnableJpaRepositories scans entities; cannot distinguish entity package from repository package.

--- Q10 ---
TOPIC: Spring Boot
SCENARIO: AiPromptBuilder.build() lines 55-68:
  BeanOutputConverter<?> converter = switch (mode) { ... };
  fullUserContent = userMessage + "\n\n--- REQUIRED OUTPUT FORMAT ---\n" + converter.getFormat();
QUESTION: What does BeanOutputConverter.getFormat() produce? How does AiResponseMapper use the same converter later?
WHAT IT TESTS: Spring AI structured output; JSON schema generation from Java types.
EXPECTED ANSWER: getFormat() appends instructions/schema derived from the record/class annotations (GenerateIssuesOutput, etc.) telling the LLM to respond in parseable JSON matching the Java type. AiResponseMapper calls converter.convert(rawContent) to deserialize LLM text into typed output objects.
FOLLOW-UP: What happens if the LLM returns markdown-wrapped JSON?
RED FLAG: Says BeanOutputConverter calls the LLM; confuses with Jackson ObjectMapper only.

================================================================================
HIBERNATE / JPA (8 questions)
================================================================================

--- Q1 ---
TOPIC: Hibernate-JPA
SCENARIO: IssueEntity has @ManyToOne(fetch = FetchType.LAZY) on assignee, reporter, project, sprint. JpaRepositoryMapper.toModel(IssueEntity) calls toModel(entity.getAssignee()) at line 255.
QUESTION: When is the assignee actually loaded from the database? Describe the N+1 query pattern if you load 100 issues and map each to domain model inside one transaction.
WHAT IT TESTS: Lazy loading; N+1 problem; session-bound proxies.
EXPECTED ANSWER: Assignee loads when getAssignee() is accessed and session is open. Loading 100 issues = 1 query for issues; mapping each assignee triggers up to 100 additional SELECTs for users (N+1). Same risk for project/sprint if accessed per issue.
FOLLOW-UP: How would you fix N+1 in findByProjectId using JPQL JOIN FETCH?
RED FLAG: Says LAZY means data is never loaded; thinks @ManyToOne is always eager.

--- Q2 ---
TOPIC: Hibernate-JPA
SCENARIO: IssueEntity lines 52-55:
  @ElementCollection
  @CollectionTable(name = "issue_labels", joinColumns = @JoinColumn(name = "issue_id"))
  private List<String> labels;
  Flyway V1 lines 61-66: issue_labels (issue_id, label) composite PK.
QUESTION: Why a separate issue_labels table instead of a single labels VARCHAR column on issues? What does @ElementCollection imply for cascade and entity lifecycle?
WHAT IT TESTS: Element collection mapping; normalization; owned collection semantics.
EXPECTED ANSWER: Separate table is 1NF — multiple labels per issue without comma-separated strings (which break querying and violate atomicity). @ElementCollection means labels are owned value types, not entities — persisted/deleted with parent issue, no separate repository.
FOLLOW-UP: What SQL does Hibernate emit when you add two labels to an issue and save?
RED FLAG: Suggests storing JSON string in one column is equivalent for indexing; says labels have their own @Entity.

--- Q3 ---
TOPIC: Hibernate-JPA
SCENARIO: AutomationRuleEntity embeds trigger and action twice using RuleValueEmbeddable:
  @Embedded @AttributeOverrides({ @AttributeOverride(name = "type", column = @Column(name = "trigger_type")), ... })
  Same for action_type/action_value columns on automation_rules table.
QUESTION: Why @AttributeOverrides? What happens if you embed the same class twice without overrides?
WHAT IT TESTS: @Embeddable; column name collision; embedding vs separate tables.
EXPECTED ANSWER: Both embeddables map type/value fields — without overrides Hibernate would map both to columns named "type" and "value", causing collision. Overrides rename columns to trigger_type, trigger_value, action_type, action_value on the parent table — no extra join for trigger/action.
FOLLOW-UP: Why are conditions @ElementCollection to automation_rule_conditions instead of @Embedded?
RED FLAG: Says @Embedded creates a new table per embeddable; cannot explain column collision.

--- Q4 ---
TOPIC: Hibernate-JPA
SCENARIO: All entities use:
  @Id @GeneratedValue @UuidGenerator private UUID id;
  @Column(name = "public_id", unique = true) private String publicId;
  Domain Issue.id maps to publicId via JpaRepositoryMapper.
QUESTION: Why maintain UUID internal PK and String publicId? How does valueOrGenerated() work when creating new records?
WHAT IT TESTS: Dual identifier pattern; API stability; UUID generation strategy.
EXPECTED ANSWER: UUID PK is opaque, non-guessable, and safe for FK joins internally. publicId (e.g. issue-xxx) is stable API-facing identifier decoupled from DB surrogate. valueOrGenerated generates prefix-UUID when id is null. @UuidGenerator assigns PK before insert at application/Hibernate level.
FOLLOW-UP: What is an enumeration attack on sequential integer IDs in URLs?
RED FLAG: Says publicId is redundant and APIs should expose UUID PK directly without tradeoff discussion.

--- Q5 ---
TOPIC: Hibernate-JPA
SCENARIO: JpaIssueRepositoryAdapter.findById():
  @Transactional(readOnly = true)
  return issueJpaRepository.findByPublicId(id).map(mapper::toModel);
QUESTION: What optimizations does Hibernate apply for readOnly=true? Does it prevent accidental writes?
WHAT IT TESTS: Read-only transaction hint; flush mode; dirty checking.
EXPECTED ANSWER: readOnly=true hints no state changes — Hibernate may skip dirty checking and avoid flush on commit, reducing overhead. It does not block writes at compile time; calling save() in same transaction could still persist. Primarily optimization and documentation of intent.
FOLLOW-UP: Why is readOnly on adapter method but @Transactional on class level for writes?
RED FLAG: Says readOnly prevents all database writes including other services; thinks it adds DB user read-only privilege.

--- Q6 ---
TOPIC: Hibernate-JPA
SCENARIO: JpaRepositoryMapper.toEntity(Issue) lines 276-283 calls resolveUser(), resolveProject(), resolveSprint() which execute findByPublicId queries during mapping.
QUESTION: Is it appropriate for a mapper to perform repository lookups? What session/transaction issues can arise?
WHAT IT TESTS: Mapper side effects; transaction boundaries; separation of concerns.
EXPECTED ANSWER: Mapper resolves foreign keys by loading entities — convenient but mixes mapping with persistence lookups (SRP violation). Requires active transaction/session; N+1 if saving many issues; lazy associations must be managed entities for FK assignment. Outside transaction, LazyInitializationException possible.
FOLLOW-UP: How does resolveUser behave if assignee is null on the domain Issue?
RED FLAG: Says mapper should never touch repositories; unaware that toEntity triggers DB reads per association.

--- Q7 ---
TOPIC: Hibernate-JPA
SCENARIO: IssueJpaRepository: List<IssueEntity> findByProject_PublicId(String projectId);
  InvitationJpaRepository: findByWorkspaceIdAndEmailIgnoreCaseAndStatus(...)
QUESTION: How does Spring Data derive SQL from findByProject_PublicId? What does the underscore mean?
WHAT IT TESTS: Derived query methods; property traversal; IgnoreCase.
EXPECTED ANSWER: Spring Data parses method name into query: find By Project.PublicId → JOIN or path project.publicId = ?1. Underscore disambiguates nested property (project.publicId) from a literal "Project_PublicId" field. IgnoreCase adds LOWER() comparison for email. Status matches enum/string column.
FOLLOW-UP: What is the difference between findByProject_PublicId and findByProjectPublicId if ProjectEntity field is publicId?
RED FLAG: Says underscore is SQL JOIN syntax; thinks PublicId is a separate column on issues table.

--- Q8 ---
TOPIC: Hibernate-JPA
SCENARIO: AutomationRuleJpaRepository declares void deleteByPublicId(String publicId). JpaAutomationRuleRepositoryAdapter.deleteById() calls it.
QUESTION: Does deleteByPublicId need @Modifying? What transaction is required? Does it load the entity first?
WHAT IT TESTS: Derived delete queries; modifying queries; transaction requirements.
EXPECTED ANSWER: Spring Data derived delete typically generates DELETE FROM ... WHERE public_id = ? without loading entity — may need @Transactional on calling service/adapter. @Modifying is required for explicit @Query deletes/updates, not always for derived deletes, but transaction is mandatory or you get TransactionRequiredException.
FOLLOW-UP: How are automation_rule_conditions rows deleted when parent rule is deleted?
RED FLAG: Insists entity must be loaded first always; says delete works without any transaction.

--- Q9 ---
TOPIC: Hibernate-JPA
SCENARIO: AutomationRuleEntity conditions:
  @ElementCollection @OrderColumn(name = "position")
  Flyway: PRIMARY KEY (rule_id, position)
QUESTION: What happens to condition order if @OrderColumn is removed? How does AutomationConditionEvaluator depend on order?
WHAT IT TESTS: @OrderColumn; list ordering in collection table; AND semantics.
EXPECTED ANSWER: @OrderColumn persists list index in position column — reload preserves evaluation order. Without it, order may be undefined (hash/insert order). Evaluator ANDs conditions in list order — wrong order could change short-circuit behavior if conditions were order-dependent (currently all must pass).
FOLLOW-UP: Is condition evaluation short-circuiting on first false?
RED FLAG: Says order does not matter for AND logic without acknowledging persistence reload ambiguity.

================================================================================
MULTITHREADING (7 questions)
================================================================================

--- Q1 ---
TOPIC: Multithreading
SCENARIO: AutomationExecutionService:
  private static final ThreadLocal<AutomationExecutionContext> CONTEXT = new ThreadLocal<>();
  CONTEXT.set(ctx) in listener; CONTEXT.remove() in finally when depth <= 0.
QUESTION: Why ThreadLocal for automation depth and executed-rule tracking? What memory leak occurs if remove() is skipped in a Tomcat thread pool?
WHAT IT TESTS: ThreadLocal semantics; thread pool reuse; memory leaks.
EXPECTED ANSWER: Each Tomcat worker thread needs isolated automation chain state across nested events without passing context through every method. ThreadLocal provides per-thread storage. Pooled threads retain ThreadLocal values after request ends — next request on same thread sees stale context (wrong depth, wrong executed rules) if remove() is skipped — effective memory/state leak.
FOLLOW-UP: Why is remove() in finally, not only at normal return?
RED FLAG: Says ThreadLocal is shared across all users globally; recommends static HashMap for context.

--- Q2 ---
TOPIC: Multithreading
SCENARIO: POST /api/issues hits IssueController → IssueService.create() → publishes event → AFTER_COMMIT listener on same request thread (default Spring sync event multicaster).
QUESTION: Which thread executes onAutomationEvent? Why does ThreadLocal still work?
WHAT IT TESTS: Request thread model; synchronous event dispatch; thread affinity.
EXPECTED ANSWER: Default Spring publishes events synchronously on the calling thread after commit — same Tomcat worker thread that handled HTTP request. ThreadLocal set in listener sees same thread as HTTP handling, so context is visible without cross-thread propagation.
FOLLOW-UP: What would break if you switched to @Async event listener?
RED FLAG: Claims listener always runs on a separate thread pool without checking @Async; says ThreadLocal propagates across threads automatically.

--- Q3 ---
TOPIC: Multithreading
SCENARIO: JwtAuthenticationFilter.doFilterInternal() sets SecurityContextHolder.getContext().setAuthentication(...) then filterChain.doFilter(). On error, SecurityContextHolder.clearContext().
QUESTION: Is SecurityContextHolder thread-safe for concurrent requests? How does STATELESS JWT auth isolate users?
WHAT IT TESTS: SecurityContextHolder ThreadLocal; per-request isolation.
EXPECTED ANSWER: SecurityContextHolder stores context in ThreadLocal by default — each Tomcat thread has its own Authentication. Concurrent requests do not see each other's JWT principals. clearContext() on error prevents leaking auth to next request on pooled thread.
FOLLOW-UP: What is SecurityContextHolder.MODE_INHERITABLETHREADLOCAL used for?
RED FLAG: Says SecurityContext is global singleton shared by all requests.

--- Q4 ---
TOPIC: Multithreading
SCENARIO: IssueService.create() lines 104-109:
  long existing = issueRepository.findByProjectId(projectId).size();
  String key = project.getKey() + "-" + (100 + existing + 1);
  DB has UNIQUE on issues.key (V1 line 44).
QUESTION: Describe the race when two users create issues simultaneously. What symptom appears? How would you fix it?
WHAT IT TESTS: Read-modify-write race; lost update; DB constraint as safety net.
EXPECTED ANSWER: Both threads read same existing count, compute same key (e.g. PROJ-101), one INSERT succeeds, second fails UNIQUE constraint on key or overwrites logic incorrectly. Fix: DB sequence per project, SELECT FOR UPDATE on project row, or atomic INSERT with serial key generation.
FOLLOW-UP: Why is issueId "issue-" + currentTimeMillis() safer than key generation?
RED FLAG: Says size() is atomic; ignores UNIQUE constraint as failure mode.

--- Q5 ---
TOPIC: Multithreading
SCENARIO: REQUIRES_NEW on automation listener — same Tomcat thread, new transactional boundary after outer commit.
QUESTION: Explain transaction suspension when REQUIRES_NEW starts. Can the listener see uncommitted data from another concurrent request?
WHAT IT TESTS: Transaction propagation; isolation between requests vs transactions.
EXPECTED ANSWER: REQUIRES_NEW suspends any existing transaction (none after AFTER_COMMIT), opens new EntityManager/transaction for automation. Concurrent requests are different threads/transactions — isolation level applies per transaction; listener only sees committed issue data from its own committed publish event, not other requests' uncommitted work.
FOLLOW-UP: If automation updates the same issue, could it deadlock with another transaction?
RED FLAG: Confuses thread isolation with transaction isolation; thinks REQUIRES_NEW runs in publisher transaction.

--- Q6 ---
TOPIC: Multithreading
SCENARIO: IssueService.getAll() without projectId loads all issues then filters in stream with projectRepository.findById per issue (lines 78-83).
QUESTION: Under load with 10,000 issues and 200 Tomcat threads, what contention appears on DB connection pool and CPU?
WHAT IT TESTS: Scalability; connection pool exhaustion; parallel stream pitfalls.
EXPECTED ANSWER: Each request may load all issues (1 query) plus N project lookups — CPU for filtering and DB pressure on HikariCP pool. Many concurrent getAll() calls multiply queries and risk pool exhaustion. Not parallel streams here, but thread pool × heavy service method = throughput collapse.
FOLLOW-UP: Would .parallelStream() on issue list help?
RED FLAG: Recommends parallelStream without noting shared pool and thread safety of repositories.

--- Q7 ---
TOPIC: Multithreading
SCENARIO: AutomationExecutionContext tracks depth; increment on entry, decrement in finally; max depth from @Value stops infinite automation loops when rules trigger each other.
QUESTION: How does depth prevent infinite loops when rule A's action causes issue update that fires rule B? Is this mutex or reentrant lock?
WHAT IT TESTS: Cooperative recursion guard vs locking; automation cycle detection.
EXPECTED ANSWER: Depth counter is a soft limit per ThreadLocal chain — not OS mutex. When depth >= maxDepth, listener returns early. executedRuleIds Set prevents same rule firing twice in one chain. Different from synchronized — no blocking, just stop processing.
FOLLOW-UP: Why also check ctx.getExecutedRuleIds().contains(rule.getId())?
RED FLAG: Says depth is implemented with synchronized keyword on IssueService.

================================================================================
OOPs (7 questions)
================================================================================

--- Q1 ---
TOPIC: OOPs
SCENARIO: InvitationDeliveryService interface; NoOpInvitationDeliveryService and SmtpInvitationDeliveryService implementations selected by @ConditionalOnProperty.
QUESTION: Which OOP principle does this demonstrate? How is runtime implementation chosen?
WHAT IT TESTS: Polymorphism; dependency inversion; Strategy pattern linkage.
EXPECTED ANSWER: Dependency inversion — InvitationService depends on abstraction, not SMTP细节. Polymorphism lets sendWorkspaceInvitation behave differently per implementation. Spring chooses concrete bean at startup based on property — runtime polymorphism without if/else in InvitationService.
FOLLOW-UP: How would you unit test InvitationService without sending real email?
RED FLAG: Says this violates OOP because there are two classes; recommends if (mailEnabled) inside service.

--- Q2 ---
TOPIC: OOPs
SCENARIO: IssueRepository interface in com.jeerai.backend.repository.issue; JpaIssueRepositoryAdapter implements it behind @Profile("postgres").
QUESTION: Which SOLID principles apply? What does the service layer depend on?
WHAT IT TESTS: Dependency inversion; interface segregation; layering.
EXPECTED ANSWER: IssueService depends on IssueRepository interface (abstraction), not JPA — Dependency Inversion. Adapter implements interface — Open/Closed for new storage backends. Services do not import jakarta.persistence — separation of concerns and testability with in-memory fakes.
FOLLOW-UP: Is JpaRepositoryMapper part of the repository pattern or a separate concern?
RED FLAG: Says IssueService should inject IssueJpaRepository directly for simplicity.

--- Q3 ---
TOPIC: OOPs
SCENARIO: AutomationEvent extends ApplicationEvent with super(source) in constructor; IssueService passes `this` as source.
QUESTION: Why extend ApplicationEvent instead of a plain POJO? What does the source field represent?
WHAT IT TESTS: Inheritance for framework integration; event source convention.
EXPECTED ANSWER: Spring's event infrastructure expects ApplicationEvent (or POJO listeners in newer Spring). Extending provides timestamp, source, and compatibility with ApplicationEventPublisher. source is the publishing object (IssueService) for debugging or listener filtering — super(source) sets it on parent.
FOLLOW-UP: Could AutomationEvent be a record implementing an interface?
RED FLAG: Says inheritance is only for code reuse of business logic; unaware of Spring event type expectations.

--- Q4 ---
TOPIC: OOPs
SCENARIO: AutomationRule.RuleValue as public static class in domain model; AutomationRuleEntity.RuleValueEmbeddable as static nested @Embeddable.
QUESTION: Why static nested class for RuleValue? What breaks if it were a non-static inner class?
WHAT IT TESTS: Static vs inner classes; implicit outer reference; serialization/mapping.
EXPECTED ANSWER: static nested class does not hold reference to outer AutomationRule instance — can be instantiated independently, smaller memory footprint, suitable for value objects used in lists. Non-static inner class requires enclosing instance — problematic for JPA embeddables and Jackson mapping.
FOLLOW-UP: Why duplicate RuleValue and RuleValueEmbeddable instead of one class?
RED FLAG: Says inner class always shares all fields of outer class automatically.

--- Q5 ---
TOPIC: OOPs
SCENARIO: JpaRepositoryMapper ~481 lines maps User, Project, Issue, Activity, Notification, AutomationRule, Workspace, Invitation, Sprint, comments — injects 11 JPA repositories.
QUESTION: Which SOLID principle does this violate? Propose a refactor aligned with JeerAI structure.
WHAT IT TESTS: Single Responsibility Principle; god object smell.
EXPECTED ANSWER: Violates SRP — one class handles all entity↔model mapping and FK resolution. Refactor: split into IssueMapper, UserMapper, etc., or use MapStruct; keep resolve* methods in a small ReferenceResolver service injected into mappers.
FOLLOW-UP: Point to a comment in the mapper showing a past performance fix.
RED FLAG: Defends god mapper as best practice; cannot name SRP.

--- Q6 ---
TOPIC: OOPs
SCENARIO: WorkspaceAccessService used by IssueService, ProjectService, AnalyticsService, SprintService, AiContextService, etc. for requireProjectReadAccess and canCurrentUser.
QUESTION: Does centralizing access control in one service violate Open/Closed? How does it help?
WHAT IT TESTS: Cross-cutting concern; facade; DRY vs OCP tradeoff.
EXPECTED ANSWER: Centralizing authorization is a Facade — new permission rules change one service (modification closed to callers). Callers stay open for extension without duplicating workspace/project checks. Tradeoff: WorkspaceAccessService can grow — but better than scattering checks in every service method.
FOLLOW-UP: What happens if a developer adds a new controller method and forgets WorkspaceAccessService?
RED FLAG: Says every service should implement its own copy of permission logic for flexibility.

--- Q7 ---
TOPIC: OOPs
SCENARIO: AutomationConditionEvaluator.evaluate() loops conditions; returns false on first failing passes(); empty conditions return true.
QUESTION: Which behavioral pattern does this resemble? How would Open/Closed apply to adding a new condition type "sprint_is"?
WHAT IT TESTS: Chain of Responsibility / composite validation; switch extension.
EXPECTED ANSWER: Sequential validation with short-circuit resembles Chain of Responsibility (manual loop, not linked chain). Adding sprint_is requires modifying passes() switch — violates OCP unless refactored to Map<String, Predicate<IssueSnapshot>> or strategy registry per condition type.
FOLLOW-UP: Why does default -> true in passes() for unknown types matter?
RED FLAG: Says default should throw always without discussing backward compatibility of stored rules.

================================================================================
DBMS (8 questions)
================================================================================

--- Q1 ---
TOPIC: DBMS
SCENARIO: JeerAI schema across V1-V6 migrations: users, projects, issues, issue_labels, activities, notifications, automation_rules, automation_rule_conditions, workspaces, workspace_members, invitations, project_permissions, etc.
QUESTION: Give an example of a transitive dependency that would violate 3NF if user email were stored only on activities instead of users table.
WHAT IT TESTS: Normal forms; transitive dependencies; normalization.
EXPECTED ANSWER: If activities stored actor_email dependent on actor_id but not on activity PK, email would depend transitively on activity id through actor — anomaly on actor email change. JeerAI normalizes: activities.actor_id FK → users, email lives only on users. 3NF requires non-key attributes depend only on primary key, not other non-key attributes.
FOLLOW-UP: Is storing target_title on activities denormalization violating 3NF?
RED FLAG: Cannot define 3NF; says all denormalization is bad without audit log exception.

--- Q2 ---
TOPIC: DBMS
SCENARIO: issue_labels table (issue_id, label) vs storing labels as comma-separated VARCHAR on issues.
QUESTION: What normal form violation is comma-separated labels? Write a query to find issues with label "bug" in both designs.
WHAT IT TESTS: 1NF; queryability; update anomalies.
EXPECTED ANSWER: CSV violates 1NF (multi-valued attribute in one column). Element collection table: SELECT issue_id FROM issue_labels WHERE label = 'bug'. CSV: fragile LIKE '%,bug,%' or string_split depending on DB — poor indexing, duplicate delimiter bugs.
FOLLOW-UP: Can you index label lookups efficiently in JeerAI's design?
RED FLAG: Recommends CSV for simplicity; cannot write correct CSV query.

--- Q3 ---
TOPIC: DBMS
SCENARIO: activities table (V1 lines 79-92) stores target_id, target_key, target_title, detail denormalized. IssueService.createActivity() copies saved.getKey() and title at write time.
QUESTION: Why denormalize title/key into activities if issues table exists? What audit pattern does this support?
WHAT IT TESTS: Intentional denormalization; slowly changing dimensions; audit trail.
EXPECTED ANSWER: Activity feed shows historical snapshot — if issue title changes later, past activities still show title at event time. Avoids JOIN on every feed read and preserves audit narrative. Classic event log / activity stream pattern.
FOLLOW-UP: What if issue is deleted — should activities CASCADE?
RED FLAG: Calls all denormalization a schema mistake; says always JOIN issues for display.

--- Q4 ---
TOPIC: DBMS
SCENARIO: workspace_members: UUID id as PK AND UNIQUE(workspace_id, user_id) per V2 and WorkspaceMemberEntity @UniqueConstraint.
QUESTION: Why both a surrogate PK and composite unique? Can composite (workspace_id, user_id) alone be PK?
WHAT IT TESTS: Surrogate vs natural keys; composite uniqueness; FK references.
EXPECTED ANSWER: Surrogate UUID PK gives stable single-column reference for ORM and future FKs. Composite unique enforces business rule "one membership per user per workspace" without using composite PK. Composite PK is possible but JPA/Hibernate and FK ergonomics favor surrogate id + unique constraint.
FOLLOW-UP: What error occurs inserting duplicate membership?
RED FLAG: Says UNIQUE and PRIMARY KEY are identical in purpose.

--- Q5 ---
TOPIC: DBMS
SCENARIO: V4 adds notifications.recipient_user_id varchar(100) with index but NO foreign key to users.
QUESTION: What referential integrity is lost? Why might JeerAI accept this trade-off?
WHAT IT TESTS: FK constraints; eventual consistency; soft references by publicId.
EXPECTED ANSWER: DB cannot enforce recipient exists or cascade on user delete. JeerAI uses publicId string matching domain user ids — may avoid FK to support notifications for users not yet migrated, cross-service IDs, or simpler migrations. Trade-off: orphan notifications possible if user deleted without cleanup.
FOLLOW-UP: How does NotificationService scope reads to current user?
RED FLAG: Says FK is never needed; cannot explain orphan row risk.

--- Q6 ---
TOPIC: DBMS
SCENARIO: ActivityJpaRepository.countByProject_PublicIdAndCreatedAtAfter used in AiContextService for recentActivity (last 7 days).
QUESTION: What SQL shape does Spring Data generate? What index supports this? What would EXPLAIN ANALYZE reveal on a large activities table without index?
WHAT IT TESTS: Derived count queries; composite indexes; query plans.
EXPECTED ANSWER: SELECT COUNT(*) FROM activities a JOIN projects p ON ... WHERE p.public_id = ? AND a.created_at > ?. Index on (project_id, created_at) or (recipient pattern) helps. Without index: seq scan on activities — high cost proportional to table size.
FOLLOW-UP: Why is AnalyticsService counting in memory instead of DB for status buckets?
RED FLAG: Cannot map method name to COUNT query; says indexes never matter for COUNT.

--- Q7 ---
TOPIC: DBMS
SCENARIO: All tables use uuid PRIMARY KEY. public_id is separate unique varchar exposed in API.
QUESTION: Compare UUID PK vs BIGINT SERIAL for JeerAI's distributed/API use case. What is enumeration attack?
WHAT IT TESTS: Identifier strategies; security of sequential IDs.
EXPECTED ANSWER: UUID: non-sequential, safe to generate client-side, no hotspot on insert index (with random UUID). SERIAL: compact, index-friendly, but predictable — attackers enumerate /api/issues/1,2,3. JeerAI uses opaque UUID internally + publicId string — reduces guessing internal FKs; publicId still should not be sequential.
FOLLOW-UP: Does Hibernate @UuidGenerator produce time-ordered UUIDs?
RED FLAG: Says UUID is always slower so never use; confuses publicId with PK exposure only.

--- Q8 ---
TOPIC: DBMS
SCENARIO: Flyway migrations V1-V6; application-postgres.properties ddl-auto=validate; flyway_schema_history table.
QUESTION: What problem does Flyway solve? What happens if a developer adds a JPA field but no migration with ddl-auto=validate?
WHAT IT TESTS: Schema versioning; migration discipline; validate mode.
EXPECTED ANSWER: Flyway applies versioned SQL migrations reproducibly across environments; flyway_schema_history tracks applied scripts. ddl-auto=validate fails startup if entity schema mismatches DB — forces migration file for every schema change. Without Flyway, environments drift manually.
FOLLOW-UP: Can V1 script run twice safely?
RED FLAG: Says Hibernate auto-update is fine for production; never heard of flyway_schema_history.

================================================================================
CN — Computer Networks (7 questions)
================================================================================

--- Q1 ---
TOPIC: CN
SCENARIO: WebConfig.addCorsMappings() allows origins http://localhost:5173, http://localhost:8080, https://jeerai.netlify.app for /api/** and /auth/** with all common methods.
QUESTION: What is CORS and why does the browser block a Netlify frontend from calling localhost:5000 without these headers?
WHAT IT TESTS: Same-origin policy; preflight; server vs browser enforcement.
EXPECTED ANSWER: Browsers enforce Same-Origin Policy — JS from https://jeerai.netlify.app cannot read responses from http://localhost:5000 unless server sends Access-Control-Allow-Origin matching the request Origin. CORS is browser-side; Postman/curl ignore it. Preflight OPTIONS checks method/headers before POST with Authorization.
FOLLOW-UP: Is CORS authentication or authorization?
RED FLAG: Says CORS protects server from hackers like a firewall; thinks backend blocks all cross-origin at TCP level.

--- Q2 ---
TOPIC: CN
SCENARIO: WebConfig line 20: .allowCredentials(true) with explicit allowedOrigins (not "*").
QUESTION: Why can't you use allowedOrigins("*") with allowCredentials(true)? What cookie/credential behavior is affected?
WHAT IT TESTS: CORS credentials spec; wildcard restriction.
EXPECTED ANSWER: CORS spec forbids Access-Control-Allow-Origin: * when credentials included — browser requires exact origin echo. Credentials include cookies, Authorization headers treated as credentialed requests in some cases, TLS client certs. JeerAI uses JWT in Authorization — withCredentials on frontend requires explicit origin.
FOLLOW-UP: Does JeerAI use cookies for JWT?
RED FLAG: Says wildcard works fine with credentials; confuses CORS with CSRF.

--- Q3 ---
TOPIC: CN
SCENARIO: PrivateNetworkAccessFilter responds to Access-Control-Request-Private-Network: true with Access-Control-Allow-Private-Network: true. Comment: https Netlify → http localhost:5000.
QUESTION: What is Private Network Access? When does Chrome send this preflight header?
WHAT IT TESTS: PNA / CORS-RFC1918; public-to-private requests.
EXPECTED ANSWER: PNA blocks public HTTPS sites from calling private/loopback IPs until server opts in. Chrome sends Access-Control-Request-Private-Network on preflight when public origin targets private network address. Filter allows local dev against production frontend hosted on Netlify.
FOLLOW-UP: Why placed before CorsFilter in SecurityConfig?
RED FLAG: Has never heard of PNA; thinks it replaces JWT.

--- Q4 ---
TOPIC: CN
SCENARIO: WebConfig allows http://localhost:5173 but production frontend is https://jeerai.netlify.app. A developer uses http://jeerai.netlify.app by mistake.
QUESTION: Are http and https://jeerai.netlify.app the same origin? What CORS error appears?
WHAT IT TESTS: Origin tuple (scheme, host, port); debugging CORS.
EXPECTED ANSWER: Different origins — scheme differs. Browser sends Origin: http://jeerai.netlify.app; server only allows https variant — no matching Allow-Origin — browser blocks response with CORS error in console, status may appear as network error to JS.
FOLLOW-UP: How do you debug this vs 401 from JWT filter?
RED FLAG: Says only host must match; ignores scheme.

--- Q5 ---
TOPIC: CN
SCENARIO: JwtAuthenticationFilter reads HttpHeaders.AUTHORIZATION, expects "Bearer " prefix, substring(7) for token. Missing header → 401 via AuthenticationEntryPoint.
QUESTION: Why Authorization header instead of cookie for JWT in JeerAI? What is the first line of HTTP request anatomy?
WHAT IT TESTS: HTTP message structure; header choice for SPA JWT.
EXPECTED ANSWER: Request line: METHOD path HTTP/version. Headers like Authorization: Bearer <jwt>. SPAs on different origin often prefer Authorization header over HttpOnly cookies to avoid CSRF complexity and ease cross-origin APIs — JeerAI is STATELESS without session cookie.
FOLLOW-UP: Why does filter return immediately without calling chain when header missing?
RED FLAG: Says JWT should be in query string; puts token in cookie without Secure/HttpOnly discussion.

--- Q6 ---
TOPIC: CN
SCENARIO: IssueController and others use @RequestMapping(produces = MediaType.APPLICATION_JSON_VALUE). Client sends Accept: application/json.
QUESTION: How do Content-Type and Accept differ? Which does Spring use to pick HttpMessageConverter for response?
WHAT IT TESTS: HTTP content negotiation; request vs response headers.
EXPECTED ANSWER: Content-Type describes request body format (POST JSON). Accept tells server preferred response format. produces= on mapping sets response Content-Type; Spring uses Accept and produces to select converter (Jackson for JSON). Mismatch may yield 406 Not Acceptable.
FOLLOW-UP: What Content-Type does POST /api/issues expect?
RED FLAG: Confuses Accept with Content-Type; says produces sets request parsing.

--- Q7 ---
TOPIC: CN
SCENARIO: Spring Boot embedded Tomcat listens on port 5000 (application.properties). Request path: browser → Tomcat → SecurityFilterChain → DispatcherServlet → IssueController.
QUESTION: Describe HTTP request lifecycle from TCP connection to controller method invocation.
WHAT IT TESTS: Embedded server; servlet filter chain; dispatcher servlet.
EXPECTED ANSWER: TCP handshake → Tomcat acceptor assigns worker thread → parses HTTP → servlet container invokes filter chain (PNA, CORS, JWT, security) → DispatcherServlet maps URL to @PostMapping handler → argument resolvers bind JSON body → controller method runs.
FOLLOW-UP: Where does TLS terminate if deployed behind Netlify/load balancer?
RED FLAG: Says Spring creates new thread per packet; controller reached before filters.

================================================================================
OS (7 questions)
================================================================================

--- Q1 ---
TOPIC: OS
SCENARIO: Spring Boot embedded Tomcat defaults (no server.tomcat.threads.* in application.properties). Each API request uses a worker thread from the pool.
QUESTION: What is the difference between a process and a thread in this context? How does Tomcat bound concurrent request handling?
WHAT IT TESTS: Process vs thread; thread pool; blocking servlet model.
EXPECTED ANSWER: JVM runs as one OS process; Tomcat worker threads are threads sharing heap. Pool size caps concurrent request processing — excess requests queue at connector. Blocking servlet model holds thread for entire request including DB and automation listener.
FOLLOW-UP: What happens if all threads are busy and queue fills?
RED FLAG: Says each HTTP request is a new process; unlimited threads created per request.

--- Q2 ---
TOPIC: OS
SCENARIO: ThreadLocal<AutomationExecutionContext> in automation — JVM implementation stores map per thread.
QUESTION: How does ThreadLocal relate to OS threads? Where is the data stored vs heap objects referenced?
WHAT IT TESTS: Thread-local storage; JVM Thread class; GC roots.
EXPECTED ANSWER: Each Thread has a ThreadLocalMap — keys are ThreadLocal instances, values are AutomationExecutionContext objects on heap. ThreadLocal variable is a pointer; actual context object is heap GC'd when thread dies and entry removed. Pool reuse requires remove() or value outlives logical request scope.
FOLLOW-UP: Can two ThreadLocals on same thread hold different values?
RED FLAG: Says ThreadLocal data lives in CPU register only.

--- Q3 ---
TOPIC: OS
SCENARIO: @Transactional issue update and automation REQUIRES_NEW — PostgreSQL row-level locks on UPDATE issues.
QUESTION: Compare mutex vs semaphore. How does DB row lock relate to Java synchronized?
WHAT IT TESTS: Locking primitives; DB vs in-process synchronization.
EXPECTED ANSWER: Mutex allows one owner; semaphore allows N. Row-level lock in PostgreSQL is like mutex for that row during transaction — unrelated to Java synchronized unless using explicit JDBC. Two JVM threads updating same issue — DB serializes via row lock; synchronized would only help if both in same JVM coordinating same object monitor.
FOLLOW-UP: Does @Transactional(readOnly=true) take any locks?
RED FLAG: Says synchronized on IssueService method replaces DB transactions.

--- Q4 ---
TOPIC: OS
SCENARIO: JwtUtil validates expiration; ExpiredJwtException caught in filter. Domain uses Instant.now() for timestamps.
QUESTION: CPU time vs wall clock time — why Instant for expiry comparison context?
WHAT IT TESTS: Wall-clock time; timezone; monotonic clock distinction.
EXPECTED ANSWER: Token expiry is wall-clock (real-world time) — Instant represents UTC instant correctly. CPU time measures process execution, irrelevant for JWT expiry. System.currentTimeMillis() similar to Instant epoch but Instant API clearer and immutable. Clock skew between servers affects validation.
FOLLOW-UP: Should you use System.nanoTime() for JWT expiry?
RED FLAG: Recommends nanoTime for expiry; confuses monotonic with wall clock.

--- Q5 ---
TOPIC: OS
SCENARIO: SystemUsersInitializer implements ApplicationRunner; run() after context startup creates automation user; catches Exception so startup continues.
QUESTION: Where does ApplicationRunner sit in JVM/Spring Boot startup sequence? What if run() threw uncaught?
WHAT IT TESTS: Spring Boot lifecycle; startup hooks; failure modes.
EXPECTED ANSWER: Runs after application context is loaded and beans created, before app accepts traffic (part of startup). Uncaught exception would fail startup. JeerAI catches DB errors to avoid blocking deploy when DB slow — automation degrades gracefully with warning log.
FOLLOW-UP: Difference between ApplicationRunner and @PostConstruct?
RED FLAG: Says ApplicationRunner runs before Spring context exists.

--- Q6 ---
TOPIC: OS
SCENARIO: spring-boot-starter-data-jpa uses HikariCP connection pool to PostgreSQL (no explicit hikari.* config).
QUESTION: Why is opening a new DB connection expensive at OS level? What resources does pool reuse?
WHAT IT TESTS: TCP/socket cost; connection setup; pooling benefits.
EXPECTED ANSWER: New connection requires TCP handshake (and possibly TLS), PostgreSQL auth, backend process memory — involves syscalls and file descriptors. Pool reuses established connections — amortizes setup cost across requests. Without pool, each request pays full connection overhead.
FOLLOW-UP: What OS limit can exhaust under connection leak?
RED FLAG: Says connections are free; pool is only for clustering.

--- Q7 ---
TOPIC: OS
SCENARIO: AiPromptBuilder.loadSystemPrompt() uses ClassPathResource("prompts/ai-" + mode + "-system.txt") — files in src/main/resources.
QUESTION: At runtime, how does ClassPathResource locate files vs FileInputStream on /etc/jeerai/prompt.txt? What happens in a fat JAR?
WHAT IT TESTS: Classpath vs filesystem; JAR nesting; class loaders.
EXPECTED ANSWER: ClassPathResource reads from JVM classpath (resources inside JAR or target/classes), not arbitrary OS paths — uses ClassLoader.getResourceAsStream. Fat JAR packs files inside archive — cannot use File path directly without extraction. Filesystem path depends on OS directory structure and permissions.
FOLLOW-UP: Would you hot-reload prompts without restart?
RED FLAG: Says ClassPathResource reads from src/main/java folder in production.

================================================================================
DESIGN PATTERNS (10 questions)
================================================================================

--- Q1 ---
TOPIC: Design Patterns
SCENARIO: IssueRepository (domain) ← JpaIssueRepositoryAdapter ← IssueJpaRepository (Spring Data) with JpaRepositoryMapper converting IssueEntity ↔ Issue.
QUESTION: Name the patterns involved. What problem does the Adapter solve vs raw JpaRepository in services?
WHAT IT TESTS: Repository + Adapter patterns; hexagonal architecture.
EXPECTED ANSWER: Repository pattern abstracts persistence; Adapter translates between domain Issue and JPA IssueEntity/API. Services stay free of JPA annotations and Spring Data types — can swap postgres adapter for another store in tests.
FOLLOW-UP: Where does anti-corruption sit — mapper or adapter class?
RED FLAG: Calls entire stack "MVC"; says adapter and repository are identical patterns.

--- Q2 ---
TOPIC: Design Patterns
SCENARIO: InvitationDeliveryService with @ConditionalOnProperty selecting Smtp vs NoOp implementation.
QUESTION: Is this Strategy, Factory, or both? How does it differ from Factory Method pattern?
WHAT IT TESTS: Strategy vs Factory; Spring bean selection.
EXPECTED ANSWER: Strategy — interchangeable algorithms for sending invites. Spring acts as composition root selecting implementation at startup (like Simple Factory configured by property). Factory Method would delegate creation to subclass method; here bean registration replaces explicit new().
FOLLOW-UP: How is this better than enum MailType { SMTP, NOOP } with switch?
RED FLAG: Insists it is Abstract Factory because there are two classes.

--- Q3 ---
TOPIC: Design Patterns
SCENARIO: IssueService publishes AutomationEvent via ApplicationEventPublisher; AutomationExecutionService listens with @TransactionalEventListener.
QUESTION: Which pattern? Why not direct automationExecutionService.handle(event) call?
WHAT IT TESTS: Observer / publish-subscribe; decoupling; transaction boundaries.
EXPECTED ANSWER: Observer — publisher does not know listeners. Decouples IssueService from automation module, allows multiple listeners, AFTER_COMMIT phase handled declaratively. Direct call would couple layers and tempt wrong transaction timing.
FOLLOW-UP: How would you add a second listener for metrics without changing IssueService?
RED FLAG: Says events are always slower with no benefit; recommends direct calls everywhere.

--- Q4 ---
TOPIC: Design Patterns
SCENARIO: AutomationConditionEvaluator loops conditions, returns false on first failure; unknown type defaults to true in passes().
QUESTION: Chain of Responsibility vs Decorator — which fits? How refactor for Open/Closed?
WHAT IT TESTS: Behavioral pattern identification; extensibility.
EXPECTED ANSWER: Resembles Chain of Responsibility (manual for-loop, not linked handlers). Decorator would wrap Issue and add behavior — not matching. Open/Closed refactor: register ConditionHandler beans per type string, auto-wired list — add new handler class without editing evaluator.
FOLLOW-UP: Is short-circuit on first false AND or OR logic?
RED FLAG: Calls it Decorator because "conditions wrap the issue."

--- Q5 ---
TOPIC: Design Patterns
SCENARIO: AiPromptBuilder.build() constructs Prompt with system message, optional history messages, user message, returns AiPromptBundle(Prompt, converter) — overloaded build() for backward compatibility.
QUESTION: Which pattern? What is telescoping constructor anti-pattern avoided?
WHAT IT TESTS: Builder pattern; optional parameters.
EXPECTED ANSWER: Builder-style assembly (fluent steps could be added; currently single method builds complex Prompt). Avoids constructor with 8+ optional parameters (telescoping constructors). Separates construction of Prompt from AiService consumption via AiPromptBundle product.
FOLLOW-UP: Is AiPromptBuilder a true Builder without director and step methods?
RED FLAG: Confuses with Factory — says build() creates new AiPromptBuilder instances.

--- Q6 ---
TOPIC: Design Patterns
SCENARIO: BeanOutputConverter<GenerateIssuesOutput> created per mode in switch; getFormat() and convert() encapsulate schema + parsing.
QUESTION: What factory role does Spring AI's BeanOutputConverter play?
WHAT IT TESTS: Simple factory; encapsulation of object creation/conversion.
EXPECTED ANSWER: Encapsulates creation of output format instructions and parsing logic tied to Java type — hides Jackson/schema details from AiPromptBuilder. Like a factory producing configured converter per output class.
FOLLOW-UP: Could you use one BeanOutputConverter for all modes?
RED FLAG: Says BeanOutputConverter is a Spring @Bean singleton only.

--- Q7 ---
TOPIC: Design Patterns
SCENARIO: Security filter chain: PrivateNetworkAccessFilter → CorsFilter → JwtAuthenticationFilter → UsernamePasswordAuthenticationFilter (Spring default chain continues).
QUESTION: How do servlet filters relate to Decorator pattern? Why does order matter?
WHAT IT TESTS: Decorator chain; responsibility layering.
EXPECTED ANSWER: Each filter wraps the chain (delegates to FilterChain.doFilter) — classic Decorator. Order matters because outer filter runs first on way in, last on way out — PNA/CORS must handle OPTIONS before JWT rejects missing token on preflight.
FOLLOW-UP: Is JwtAuthenticationFilter a Decorator or Proxy?
RED FLAG: Says filters run in random order; JWT should be outermost always without preflight exception.

--- Q8 ---
TOPIC: Design Patterns
SCENARIO: IssueEntity assignee @ManyToOne(LAZY) — Hibernate returns proxy until accessed.
QUESTION: Explain Proxy pattern in lazy loading. What triggers initialization? What does Hibernate.initialize() do?
WHAT IT TESTS: Proxy pattern; lazy loading mechanism.
EXPECTED ANSWER: Hibernate substitutes UserEntity proxy with only id loaded. Accessing non-id field triggers SQL SELECT (initialization). Hibernate.initialize(proxy) forces load within session. Outside session → LazyInitializationException.
FOLLOW-UP: How does toModel(entity.getAssignee()) trigger proxy in mapper?
RED FLAG: Says LAZY means null assignee forever.

--- Q9 ---
TOPIC: Design Patterns
SCENARIO: All @Service and @Repository beans are singleton scope by default. IssueService single instance serves all requests.
QUESTION: Spring singleton vs GoF Singleton — differences? How can singleton break with serialization or two contexts?
WHAT IT TESTS: Spring bean scope; test contexts.
EXPECTED ANSWER: Spring singleton = one instance per ApplicationContext, not per classloader globally. GoF Singleton is class-level static. Two Spring contexts (test + main) get two IssueService instances. Serialization of singleton bean rare; prototype scope needed for per-request mutable state — JeerAI uses ThreadLocal instead for automation context.
FOLLOW-UP: Why mustn't IssueService store request-specific state in fields?
RED FLAG: Says Spring creates new IssueService per HTTP request by default.

--- Q10 ---
TOPIC: Design Patterns
SCENARIO: IssueController returns DTOs/JSON; IssueEntity has JPA annotations; Issue domain model in service layer; IssueCreateRequest for input.
QUESTION: Why three representations? What pattern separates API from persistence?
WHAT IT TESTS: DTO pattern; layered architecture; API versioning.
EXPECTED ANSWER: IssueCreateRequest — API input validation shape. Issue — domain logic. IssueEntity — persistence mapping. Prevents JPA annotations leaking to OpenAPI/JSON, allows API fields to differ from DB (publicId vs UUID), reduces accidental lazy load serialization.
FOLLOW-UP: What breaks if controller returned IssueEntity directly?
RED FLAG: Says DTOs are useless duplication; recommends exposing entities for speed.

--- Q11 ---
TOPIC: Design Patterns
SCENARIO: IssueController → IssueService → IssueRepository — business logic in IssueService (permissions, events, notifications), controller thin.
QUESTION: MVC in Spring — where does business logic belong? Fat controller anti-pattern example in JeerAI terms.
WHAT IT TESTS: MVC / layered architecture; separation of concerns.
EXPECTED ANSWER: Controller handles HTTP mapping/status codes only. Service orchestrates repositories, access control, events — IssueService.create() is 40+ lines of business rules. Fat controller would put @Transactional, repository calls, and event publish in IssueController — untestable and mixed concerns.
FOLLOW-UP: Is WorkspaceAccessService part of controller, service, or cross-cutting?
RED FLAG: Recommends moving all logic to controller to reduce classes.

--- Q12 ---
TOPIC: Design Patterns
SCENARIO: Constructor injection of IssueRepository, WorkspaceAccessService, ApplicationEventPublisher in IssueService — no field @Autowired.
QUESTION: Dependency Injection vs Service Locator — why DI makes unit testing easier in JeerAI?
WHAT IT TESTS: DI pattern; test doubles; explicit dependencies.
EXPECTED ANSWER: Constructor lists all collaborators — tests pass mock IssueRepository and mock ApplicationEventPublisher without Spring context. Service Locator (static getBean()) hides dependencies and makes tests depend on global registry.
FOLLOW-UP: How would you verify AutomationEvent published in a unit test?
RED FLAG: Says @Autowired field injection is required for mocks to work.

================================================================================
END OF DOCUMENT
Total: 70 scenario-based questions across 9 topics
Source codebase: c:\Users\VRAJ SHAH\Desktop\jeera\backend\jeerai-backend\
================================================================================
